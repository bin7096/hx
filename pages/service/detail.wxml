<view style="width:100%;background-color:#FFFFFF">
    <view class="line" wx:for="{{list}}" wx:for-item="l" wx:key="id">
        <view class="title">*{{l.title}}</view>
        <view class="value">{{l.value}}</view>
    </view>
</view>